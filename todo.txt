-[x] fix my light/dark mode refactor in /script.js
-[x] update projects
-[x] add tag filtering to project
-[x] add the other two telegram bots:
    -[x] fmi
    -[x] ChatBot
-[ ] edit the resume link so that it opens the right language
-[ ] create a resume landing page (heck, maybe just replace the about section with it)
-[ ] add my certificate
-[ ] fix the tags in place — so that the bottom of the page doesn't jolt the whole view arround from toggling the tags
-[ ] create detailed page of project (instead of just a redirect to github/pages)